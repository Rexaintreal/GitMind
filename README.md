# GitMind
GitMind is a background AI agent that auto-commits your code while you stay in flow. It watches your codebase, detects changes, and generates clean commit messages using an LLM. No more messy git history just consistent, meaningful commits with zero effort.
